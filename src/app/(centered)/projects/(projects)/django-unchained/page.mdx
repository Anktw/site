export const metadata = {
    title: "Django Unchained | Projects | Ankit Tiwari",
    description: "A Django-based multi-tenant backend with isolated JWT auth per tenant, production setup via Docker + Gunicorn.",
}

> Under Construction